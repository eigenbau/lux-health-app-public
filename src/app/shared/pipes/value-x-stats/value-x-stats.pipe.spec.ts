import { ValueXStatsPipe } from './value-x-stats.pipe';

describe('ValueXStatsPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueXStatsPipe();
    expect(pipe).toBeTruthy();
  });
});
