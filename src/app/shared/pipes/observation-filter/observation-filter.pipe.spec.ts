import { ObservationFilterPipe } from './observation-filter.pipe';

describe('ObservationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ObservationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
