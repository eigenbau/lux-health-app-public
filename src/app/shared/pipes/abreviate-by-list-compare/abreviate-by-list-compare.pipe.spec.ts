import { AbreviateByListComparePipe } from './abreviate-by-list-compare.pipe';

describe('AbreviateByListComparePipe', () => {
  it('create an instance', () => {
    const pipe = new AbreviateByListComparePipe();
    expect(pipe).toBeTruthy();
  });
});
