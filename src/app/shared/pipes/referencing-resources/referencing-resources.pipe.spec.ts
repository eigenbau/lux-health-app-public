import { ReferencingResourcesPipe } from './referencing-resources.pipe';

describe('ReferencingResourcesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReferencingResourcesPipe();
    expect(pipe).toBeTruthy();
  });
});
