import { ValueCodeFormatPipe } from './value-code-format.pipe';

describe('ValueCodeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueCodeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
