import { ReferencedResourceTypePipe } from './referenced-resource-type.pipe';

describe('ReferencedResourceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ReferencedResourceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
