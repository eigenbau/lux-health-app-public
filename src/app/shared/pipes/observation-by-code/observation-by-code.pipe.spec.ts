import { ObservationByCodePipe } from './observation-by-code.pipe';

describe('ObservationByCodePipe', () => {
  it('create an instance', () => {
    const pipe = new ObservationByCodePipe();
    expect(pipe).toBeTruthy();
  });
});
