import { ReferencedResourcePipe } from './referenced-resource.pipe';

describe('ReferencedResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new ReferencedResourcePipe();
    expect(pipe).toBeTruthy();
  });
});
