import { ApexchartsSeriesValuePipe } from './apexcharts-series-value.pipe';

describe('ApexchartsSeriesValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ApexchartsSeriesValuePipe();
    expect(pipe).toBeTruthy();
  });
});
