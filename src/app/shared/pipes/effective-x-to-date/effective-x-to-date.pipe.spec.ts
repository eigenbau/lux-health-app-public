import { EffectiveXToDatePipe } from './effective-x-to-date.pipe';

describe('EffectiveXToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new EffectiveXToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
