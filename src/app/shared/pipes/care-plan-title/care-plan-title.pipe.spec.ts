import { CarePlanTitlePipe } from './care-plan-title.pipe';

describe('CarePlanTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CarePlanTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
