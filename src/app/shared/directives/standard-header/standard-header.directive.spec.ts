import { StandardHeaderDirective } from './standard-header.directive';

describe('StandardHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new StandardHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
