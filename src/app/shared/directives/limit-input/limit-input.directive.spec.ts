import { LimitInputDirective } from './limit-input.directive';

describe('TelecomDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitInputDirective();
    expect(directive).toBeTruthy();
  });
});
