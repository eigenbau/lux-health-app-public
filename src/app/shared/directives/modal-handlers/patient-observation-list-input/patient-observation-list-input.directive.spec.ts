import { PatientObservationListInputDirective } from './patient-observation-list-input.directive';

describe('PatientObservationListInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientObservationListInputDirective();
    expect(directive).toBeTruthy();
  });
});
