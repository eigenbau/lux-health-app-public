import { PatientDocumentReferenceInputDirective } from './patient-document-reference-input.directive';

describe('PatientDocumentReferenceInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientDocumentReferenceInputDirective();
    expect(directive).toBeTruthy();
  });
});
