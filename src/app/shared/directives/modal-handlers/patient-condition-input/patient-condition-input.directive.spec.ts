import { PatientConditionInputDirective } from './patient-condition-input.directive';

describe('PatientConditionInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientConditionInputDirective();
    expect(directive).toBeTruthy();
  });
});
