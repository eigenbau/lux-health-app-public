import { TemplateSelectionDirective } from './template-selection.directive';

describe('TemplateSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
