import { TemplateObservationInputDirective } from './template-observation-input.directive';

describe('TemplateObservationInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateObservationInputDirective();
    expect(directive).toBeTruthy();
  });
});
