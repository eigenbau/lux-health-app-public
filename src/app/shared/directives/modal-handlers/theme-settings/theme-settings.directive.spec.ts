import { ThemeSettingsDirective } from './theme-settings.directive';

describe('ThemeSettingsDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeSettingsDirective();
    expect(directive).toBeTruthy();
  });
});
