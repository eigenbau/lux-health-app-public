import { PatientProcedureInputDirective } from './patient-procedure-input.directive';

describe('PatientProcedureInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientProcedureInputDirective();
    expect(directive).toBeTruthy();
  });
});
