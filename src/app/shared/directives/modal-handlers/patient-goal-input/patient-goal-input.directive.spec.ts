import { PatientGoalInputDirective } from './patient-goal-input.directive';

describe('PatientGoalInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientGoalInputDirective();
    expect(directive).toBeTruthy();
  });
});
