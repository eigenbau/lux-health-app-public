import { PatientCarePlanInputDirective } from './patient-care-plan-input.directive';

describe('PatientCarePlanInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientCarePlanInputDirective();
    expect(directive).toBeTruthy();
  });
});
