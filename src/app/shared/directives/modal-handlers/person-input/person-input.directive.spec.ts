import { PersonInputDirective } from './person-input.directive';

describe('PersonInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PersonInputDirective();
    expect(directive).toBeTruthy();
  });
});
