import { PatientEncounterInputDirective } from './patient-encounter-input.directive';

describe('PatientEncounterInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientEncounterInputDirective();
    expect(directive).toBeTruthy();
  });
});
