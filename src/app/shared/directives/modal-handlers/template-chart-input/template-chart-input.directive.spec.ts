import { TemplateChartInputDirective } from './template-chart-input.directive';

describe('TemplateChartInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateChartInputDirective();
    expect(directive).toBeTruthy();
  });
});
