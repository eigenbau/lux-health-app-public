import { PersonDetailsDirective } from './person-details.directive';

describe('PersonDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new PersonDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
