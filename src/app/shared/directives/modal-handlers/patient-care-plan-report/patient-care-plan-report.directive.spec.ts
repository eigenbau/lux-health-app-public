import { PatientCarePlanReportDirective } from './patient-care-plan-report.directive';

describe('PatientCarePlanReportDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientCarePlanReportDirective();
    expect(directive).toBeTruthy();
  });
});
