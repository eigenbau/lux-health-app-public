import { PatientObservationInputDirective } from './patient-observation-input.directive';

describe('PatientObservationInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientObservationInputDirective();
    expect(directive).toBeTruthy();
  });
});
