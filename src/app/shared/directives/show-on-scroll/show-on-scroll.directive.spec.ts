import { ShowOnScrollDirective } from './show-on-scroll.directive';

describe('ShowOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
